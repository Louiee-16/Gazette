DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=*

DB_NAME=Final_LePMITS
DB_USER=gazette
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5432

# Shared with LePMITS's GAZETTE_SYNC_SECRET — must match exactly, or every
# sync push gets rejected. Leave unset locally unless actually testing the
# sync endpoint; it fails closed (rejects everything) when blank.
GAZETTE_SYNC_SECRET=change-me
